#pragma once

// Copy to secrets.h and fill in. secrets.h is gitignored.
#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-password"
#define MDNS_HOSTNAME "esp32cam"

// Without this, anyone on the network can reflash the camera.
#define OTA_PASSWORD "pick-something"
